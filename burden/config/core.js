/* Core configurations.
 */

//const path = require('path');
//const basedir = path.resolve(path.dirname(require.main.filename) + "/../") + "/";

module.exports = {
  app: 'YourAppName',
  env: 'devel'
}
